Commands in the form of BASH script used to compile packages.

Note that ever since the arrival of package managers in various
Linux distributions (e.g. Ubuntu, Redhat, Fedora Core),
I no longer compile packages myself. But they are still
ways to solve problems.
