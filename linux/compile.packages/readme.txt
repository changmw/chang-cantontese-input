Commands in the form of BASH script used to compile packages.

Note that ever since the arrival of package managers in distributions
Ubuntu than Redhat than Fedora, I no longer compile packages myself.
